# Java-Shape-Polymorphism
This Java program demonstrates method overriding using a base Shape class and subclasses Circle, Rectangle, and Triangle. Each subclass overrides the calculateArea() method to compute its specific area, showcasing runtime polymorphism and code reusability in object-oriented programming.
